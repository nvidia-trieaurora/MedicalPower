{
  "name": "@medicalpower/ohif-shell",
  "version": "0.1.0",
  "private": true,
  "description": "OHIF Viewer with MedicalPower custom extensions and MONAI Label integration",
  "scripts": {
    "build": "bash build.sh",
    "clean": "rm -rf dist"
  }
}
