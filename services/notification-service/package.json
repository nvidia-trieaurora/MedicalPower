{
  "name": "@medicalpower/notification-service",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-channel notification delivery (email, in-app, WebSocket)",
  "scripts": {
    "dev": "nest start --watch",
    "build": "nest build",
    "start": "node dist/main",
    "lint": "eslint \"{src,apps,libs,test}/**/*.ts\"",
    "test": "jest"
  }
}
