{
  "name": "@medicalpower/annotation-service",
  "version": "0.1.0",
  "private": true,
  "description": "Annotation session and artifact management, versioning, provenance",
  "scripts": {
    "dev": "nest start --watch",
    "build": "nest build",
    "start": "node dist/main",
    "lint": "eslint \"{src,apps,libs,test}/**/*.ts\"",
    "test": "jest"
  }
}
