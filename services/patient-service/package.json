{
  "name": "@medicalpower/patient-service",
  "version": "0.1.0",
  "private": true,
  "description": "Patient registry, demographics, identifier management",
  "scripts": {
    "dev": "nest start --watch",
    "build": "nest build",
    "start": "node dist/main",
    "lint": "eslint \"{src,apps,libs,test}/**/*.ts\"",
    "test": "jest"
  }
}
