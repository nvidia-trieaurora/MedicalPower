{
  "name": "@medicalpower/study-service",
  "version": "0.1.0",
  "private": true,
  "description": "DICOMweb proxy and study metadata cache",
  "scripts": {
    "dev": "nest start --watch",
    "build": "nest build",
    "start": "node dist/main",
    "lint": "eslint \"{src,apps,libs,test}/**/*.ts\"",
    "test": "jest"
  }
}
